<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>HFK</title>
  <icon>https://www.gravatar.com/avatar/800256e9c103ade2e1f24b330ae47d1e</icon>
  <subtitle>欢迎到来~</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://www.hfkstackoverflow.top/"/>
  <updated>2019-04-09T14:36:55.525Z</updated>
  <id>http://www.hfkstackoverflow.top/</id>
  
  <author>
    <name>HFK</name>
    <email>huang0010445341@163.com</email>
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Html学习笔记</title>
    <link href="http://www.hfkstackoverflow.top/2019/03/14/Html%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/"/>
    <id>http://www.hfkstackoverflow.top/2019/03/14/Html学习笔记/</id>
    <published>2019-03-14T09:31:10.000Z</published>
    <updated>2019-04-09T14:36:55.525Z</updated>
    
    <content type="html"><![CDATA[<blockquote><h1 id="标题-lt-h1-gt-lt-h1-gt"><a href="#标题-lt-h1-gt-lt-h1-gt" class="headerlink" title="标题 (&lt;h1&gt; &lt;/h1&gt;)"></a>标题 (<code>&lt;h1&gt; &lt;/h1&gt;</code>)</h1><p>1.大多数HTML元素都有一个 开始标签 和一个 结束标签 。</p><p>开始标签看起来像这样： <code>&lt;h1&gt;</code></p><p>结束标签看起来像这样： <code>&lt;/h1&gt;</code></p><p>h1 元素通常用于主标题，而 h2 元素通常用于副标题。还有 h3，h4，h5 和 h6 元素来表示不同的和新的部分。</p><pre><code>&lt;h1&gt;Hello World&lt;/h1&gt;&lt;h2&gt;CatPhotoApp&lt;/h2&gt;</code></pre> <a id="more"></a><h1 id="标签-lt-p-gt-lt-p-gt-）"><a href="#标签-lt-p-gt-lt-p-gt-）" class="headerlink" title="标签 (&lt;p&gt; &lt;/p&gt;）"></a>标签 (<code>&lt;p&gt; &lt;/p&gt;</code>）</h1><p>p 元素是网站上正常段落文本的首选元素。P是“paragraph”的缩写。</p><p>你可以创建一个这样的p元素：<code>&lt;p&gt;</code>我是一个p标签!<code>&lt;/p&gt;</code></p><h1 id="注释"><a href="#注释" class="headerlink" title="注释"></a>注释</h1><p>注释是一种方式，你可以在代码中留下注释，而不会影响代码本身。</p><p>注释也是使代码无效而不必完全删除的便捷方式。你可以使用 <code>&lt;!--</code> 开始添加注释，并用 <code>--&gt;</code> 结束注释。</p><h1 id="占位符"><a href="#占位符" class="headerlink" title="占位符"></a>占位符</h1><p><code>lorem ipsum text</code> 用作占位符文本。占位符就是一些文字占着位置，没有实际意义。</p><h1 id="更换文本的颜色"><a href="#更换文本的颜色" class="headerlink" title="更换文本的颜色"></a>更换文本的颜色</h1><p>修改 h2 元素的 style (样式)</p><p>以下是将你的 h2 元素的文本颜色设置为蓝色的示例：<br><code>&lt;h2 style=&quot;color: blue&quot;&gt;CatPhotoApp&lt;/h2&gt;</code></p><h1 id="使用CSS选择器定义标签"><a href="#使用CSS选择器定义标签" class="headerlink" title="使用CSS选择器定义标签"></a>使用CSS选择器定义标签</h1><p>使用CSS，您可以使用数百种CSS属性来更改元素在页面上的显示方式。</p><p>当你输入<code>&lt;h2 style=&quot;color: red&quot;&gt;CatPhotoApp&lt;/h2&gt;</code>时，你就给h2元素添加了<code>inline style(内联样式)</code>。</p><p>这是添加元素的样式的一种方法，但更好的方法是使用<code>CSS</code>，它代表<code>(Cascading Style Sheets)</code>层叠样式表。</p><p>在代码的顶端，创建一个如下所示的<code>style</code>元素，：</p><pre><code>&lt;style&gt;    &lt;/style&gt;</code></pre><p>在这个<code>style</code>元素的内部, 你可以为所有h2元素创建一个<code>CSS</code>选择器。例如，如果你希望所有的h2元素都设置为红色, 则你的样式元素将如下所示：</p><pre><code>&lt;style&gt;      选择器 {属性名称: 属性值;}      h2 {color: red;}    &lt;/style&gt;</code></pre><p>下面是一个CSS类声明的例子：</p><pre><code>&lt;style&gt;      .blue-text {        color: blue;              字体颜色    font-size: 16px;          字体大小     font-family: Monospace;   字体  }    &lt;/style&gt;</code></pre><p>你可以看到我们已经在 <code>&lt;style&gt;</code> 标签中创建了一个名为 <code>blue-text</code> 的CSS类。</p><p>你可以将类应用于HTML元素，如下所示：</p><p><code>&lt;h2 class=&quot;blue-text&quot;&gt;CatPhotoApp&lt;/h2&gt;</code></p><p>请注意，在CSS样式元素中，类选择器应该添加<code>.</code>为前缀。而在HTML元素的类声明中，类属性不能添加<code>.</code>为前缀。</p><p>请记住，你可以通过在相关元素的开始标签中使用 <code>class=&quot;your-class-here&quot;</code> 将类附加到HTML元素。</p><p>字体大小由 font-size CSS属性控制，如下所示：</p><pre><code>h1 {      font-size: 30px;    }</code></pre><p>在所有浏览器中都有几种可用的默认字体。这些包括 Monospace 、Serif 和 Sans-Serif。</p><p>当某种字体不可用时，你可以让浏览器将其 “降级” 为另一种字体。</p><p>例如，如果你希望元素使用 Helvetica 字体，但是当 Helvetica 不可用时也会降级为Sans-Serif 字体，则可以使用此CSS样式：</p><pre><code>p {      font-family: Helvetica, Sans-Serif;    }</code></pre><h1 id="引入Google字体"><a href="#引入Google字体" class="headerlink" title="引入Google字体"></a>引入Google字体</h1><p>现在, 让我们引入并应用 google 字体 (请注意, 如果 google 在你的国家被阻止, 你需要跳过这一挑战)。</p><p>首先，你需要用 link 标签来引入谷歌Lobster字体。</p><p>复制以下代码段并将其粘贴到代码编辑器的顶部：</p><pre><code>&lt;link href=&quot;https://fonts.googleapis.com/css?family=Lobster&quot; rel=&quot;stylesheet&quot; type=&quot;text/css&quot;&gt;</code></pre><h1 id="给页面添加图片"><a href="#给页面添加图片" class="headerlink" title="给页面添加图片"></a>给页面添加图片</h1><p>你可以使用 img 元素将图片添加到您的网站，并使用 src 属性指向一个图片的具体地址。</p><p>img 元素具有 alt 属性。alt 属性中的文本用于屏幕阅读器以提高可访问性，并且如果图像无法加载，则会显示。</p><p>请注意，img 元素是自关闭元素，不需要结束标签。</p><pre><code>&lt;img src=&quot;/statics/codecamp/images/relaxing-cat.jpg&quot; alt=&quot;your-image&quot;&gt;</code></pre><p>CSS包含一个 width 的属性，用于控制元素的宽度。就像使用字体一样，我们将使用 px（像素）来指定图片的宽度</p><p>例如，如果我们要创建一个名为 larger-image 的CSS类，把HTML元素的宽度设定为500像素，我们将使用：</p><pre><code>&lt;style&gt;      .larger-image {        width: 500px;      }    &lt;/style&gt;</code></pre><p>例：</p><pre><code>&lt;img class=&quot;smaller-image&quot; src=&quot;/statics/codecamp/images/relaxing-cat.jpg&quot;&gt;</code></pre><h1 id="CSS边框"><a href="#CSS边框" class="headerlink" title="CSS边框"></a>CSS边框</h1><p>CSS 边框具有 <code>style(样式)</code>、<code>color(颜色)</code>、<code>width(宽度)</code> 等属性。</p><p>例如，如果我们想要设定一个HTML元素的边框颜色为红色、边框宽度为5像素(px)、边框样式为实线(solid)，边框目前有尖角。我们可以用一个叫 border-radius（边框半径）的CSS属性来改变它的边框变成圆角。你可以使用像素来指定 border-radius 的属性值，也可以使用百分比。代码如下所示:</p><pre><code>&lt;style&gt;      .thin-red-border {        border-color: red;      颜色     border-width: 5px;      宽度    border-style: solid;    样式    border-radius: 10px;//50%    圆角(像素或者百分比)  }      &lt;/style&gt;</code></pre><p>请记住，你可以应用多个class到一个元素，只需要在多个class之间用空格分开即可。例如：</p><pre><code>&lt;img class=&quot;class1 class2&quot;&gt;</code></pre><h1 id="设置链接锚元素外部页面"><a href="#设置链接锚元素外部页面" class="headerlink" title="设置链接锚元素外部页面"></a>设置链接锚元素外部页面</h1><p>a元素，也叫<code>anchor（锚点）</code>元素，用于链接到当前页面之外的内容。</p><p>以下是一个例子：</p><pre><code>&lt;p&gt;这是一个a标签 &lt;a href=&quot;https://www.w3cschool.cn&quot;&gt;W3Cschool.cn&lt;/a&gt;跳转到W3Cschool.cn&lt;/p&gt;&lt;a href=&quot;https://www.w3cschool.cn&quot;&gt;cat photos&lt;/a&gt;</code></pre><h1 id="使用-符合设置固定链接"><a href="#使用-符合设置固定链接" class="headerlink" title="使用#符合设置固定链接"></a>使用#符合设置固定链接</h1><p>把你的图片嵌入到a元素中。例子如下：</p><pre><code>&lt;a href=&quot;#&quot;&gt;&lt;img src=&quot;/images/relaxing-cat.jpg&quot;&gt;&lt;/a&gt;</code></pre><p>请记住使用 # 作为元素的 href 属性, 以便将其转换为固定链接。</p><h1 id="为图片添加alt描述"><a href="#为图片添加alt描述" class="headerlink" title="为图片添加alt描述"></a>为图片添加alt描述</h1><p><code>alt</code> 属性， 是当图片无法显示时的替代文本。<code>alt</code> 属性对于盲人或视觉障碍的用户理解图片中的内容非常重要，搜索引擎也会搜索alt 属性来了解图片的内容。</p><p>总而言之，alt 属性是一个必需的属性，为页面上的图片都加上 alt 属性是好习惯。</p><p>你可以像下面例子中一样为img元素添加一个 alt 属性：</p><pre><code>&lt;img src=&quot;www.your-image-source.com/your-image.jpg&quot; alt=&quot;your alt text&quot;&gt;</code></pre><h1 id="创建项目符号无序列表"><a href="#创建项目符号无序列表" class="headerlink" title="创建项目符号无序列表"></a>创建项目符号无序列表</h1><p>HTML具有用于创建 <code>unordered lists</code>（无序列表） ，或带项目符号列表的特殊元素。</p><p>无序列表以 <code>&lt;ul&gt;</code> 元素开始，并包含一个或多个<code>&lt;li&gt;</code>元素。</p><p>例如：</p><pre><code>&lt;ul&gt;      &lt;li&gt;milk&lt;/li&gt;      &lt;li&gt;cheese&lt;/li&gt;    &lt;/ul&gt;</code></pre><h1 id="创建有序列表"><a href="#创建有序列表" class="headerlink" title="创建有序列表"></a>创建有序列表</h1><p>HTML具有用于创建<code>ordered lists（有序列表）</code>, 或数字编号列表的特殊元素。</p><p>有序列表以<code>&lt;ol&gt;</code>元素开始，并包含一个或多个<code>&lt;li&gt;</code>元素。</p><p>例如：</p><pre><code>&lt;ol&gt;      &lt;li&gt;Garfield&lt;/li&gt;      &lt;li&gt;Sylvester&lt;/li&gt;    &lt;/ol&gt;</code></pre><h1 id="创建文本输入框"><a href="#创建文本输入框" class="headerlink" title="创建文本输入框"></a>创建文本输入框</h1><p>文本输入框是获取用户输入的一种方便的方法。</p><p>你可以用如下方法创建：</p><p><code>&lt;input type=&quot;text&quot;&gt;</code></p><p>注意，input元素是自关闭的。</p><p><code>placeholder text（占位符）</code>是用户在 input 框输入任何内容之前放置在 input 框中的预定义文本。</p><p>你可以创建如下所示的占位符：</p><p><code>&lt;input type=&quot;text&quot; placeholder=&quot;this is placeholder text&quot;&gt;</code></p><h1 id="添加表单"><a href="#添加表单" class="headerlink" title="添加表单"></a>添加表单</h1><p>你可以使用HTML来构建跟服务器交互的Web表单。你可以通过在<code>form</code>元素上添加一个<code>action</code>属性来执行此操作。</p><p><code>action</code>属性的值指定了表单提交到服务器的地址。</p><p>例如：</p><pre><code>&lt;form action=&quot;/url-where-you-want-to-submit-form-data&quot;&gt;&lt;/form&gt;</code></pre><p>把你的文本输入框嵌套到form元素中。并为此form元素添加action=”/submit-cat-photo”。</p><pre><code>&lt;form  action=&quot;/submit-cat-photo&quot;&gt;    &lt;input type=&quot;text&quot; placeholder=&quot;cat photo URL&quot;&gt;    &lt;input type=&quot;text&quot; required&gt;  设置必填&lt;/form&gt;</code></pre><h1 id="为表单添加提交按钮"><a href="#为表单添加提交按钮" class="headerlink" title="为表单添加提交按钮"></a>为表单添加提交按钮</h1><p>我们在form中添加一个 submit (提交)按钮。点击此按钮，表单中的数据将会被发送到你使用表单 <code>action</code> 属性指定的地址上。</p><p>以下是一个submit按钮的例子：</p><pre><code>&lt;button type=&quot;submit&quot;&gt;this button submits the form&lt;/button&gt;</code></pre><h1 id="添加单选框"><a href="#添加单选框" class="headerlink" title="添加单选框"></a>添加单选框</h1><p>单选按钮是 <code>input</code> 输入框的一种类型。</p><p>每个单选按钮都应该嵌套在自己的 <code>label(标签)</code> 元素中。</p><p>所有关联的单选按钮应具有相同的 <code>name</code> 属性。</p><p>下面是一个单选按钮的例子：</p><pre><code>&lt;label&gt;&lt;input type=&quot;radio&quot; name=&quot;indoor-outdoor&quot; checked&gt; Indoor&lt;/label&gt; //checked表示选中</code></pre><h1 id="添加复选框"><a href="#添加复选框" class="headerlink" title="添加复选框"></a>添加复选框</h1><p><code>checkboxes（复选按钮）</code>通常用于可能有多个答案的问题的形式。</p><p>复选按钮是 <code>input</code> 的输入框的一种类型。</p><p>每一个复选按钮都应嵌套在其自己的 <code>label</code>元素中。</p><p>所有关联的复选按钮输入应该具有相同的 <code>name</code>属性。</p><p>以下是一个复选按钮的示例：</p><pre><code>&lt;label&gt;&lt;input type=&quot;checkbox&quot; name=&quot;personality&quot;&gt; Loving&lt;/label&gt;</code></pre><h1 id="在div元素中嵌套多个元素"><a href="#在div元素中嵌套多个元素" class="headerlink" title="在div元素中嵌套多个元素"></a>在div元素中嵌套多个元素</h1><p><code>div</code> 元素，也被称作division(层)元素，是一个盛装其他元素的通用容器。</p><p><code>div</code> 元素是最常用的HTML元素。所以可以利用CSS的继承关系把 <code>div</code> 上的CSS传递给它所有子元素。</p><p>你可以使用<code>&lt;div&gt;</code>来标记一个div元素的开始，并使用<code>&lt;/div&gt;</code>来标记一个div元素的结束。</p><pre><code>&lt;div class=&quot;gray-background&quot;&gt;//设置背景&lt;p&gt;Things cats love:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;cat nip&lt;/li&gt;&lt;li&gt;laser pointers&lt;/li&gt;&lt;li&gt;lasagna&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;Top 3 things cats hate:&lt;/p&gt;&lt;ol&gt;&lt;li&gt;flea treatment&lt;/li&gt;&lt;li&gt;thunder&lt;/li&gt;&lt;li&gt;other cats&lt;/li&gt;&lt;/ol&gt;&lt;/div&gt;</code></pre><h1 id="为标签添加ID属性"><a href="#为标签添加ID属性" class="headerlink" title="为标签添加ID属性"></a>为标签添加ID属性</h1><p>除了<code>class</code>属性之外，每一个 HTML 元素也可以具有<code>id</code>属性。</p><p>使用<code>id</code>属性有很多好处，一旦你开始使用jQuery，你将了解更多信息。</p><p><code>id</code>属性应该是唯一的。虽然浏览器不会强制唯一，但这是被广泛认可的。所以请不要给一个以上的元素相同的<code>id</code>属性。</p><p>以下是一个例子，说明如何设置<code>h2</code>元素的<code>id</code>属性为<code>cat-photo-app</code>。</p><pre><code>&lt;h2 id=&quot;cat-photo-app&quot;&gt;</code></pre><h3 id="使用ID属性设置标签样式"><a href="#使用ID属性设置标签样式" class="headerlink" title="使用ID属性设置标签样式"></a>使用ID属性设置标签样式</h3><p><code>id</code>属性的一个很酷的事情是，像类选择器一样，你可以使用CSS来设计样式。</p><p>以下是一个示例，说明如何使用 <code>cat-photo-element</code> 的id属性来获取元素 ，并设置背景颜色为绿色。在你的<code>style</code> 元素中：</p><pre><code>#cat-photo-element {      background-color: green;    }</code></pre><p>请注意，在你的<code>style</code>元素中，定义类选择器必须添加<code>.</code>为前缀，定义ID选择器必须添加<code>#</code>为前缀。</p><h1 id="使用padding布局页面标签"><a href="#使用padding布局页面标签" class="headerlink" title="使用padding布局页面标签"></a>使用padding布局页面标签</h1><p>三个重要的属性控制每个HTML元素的布局:<code>padding(内边距)、margin(外边距)、border(边框)</code></p><p>元素的<code>padding</code>控制元素与其边框<code>border</code>之间的距离。</p><p>元素的 <code>margin （外边距）</code>控制元素 <code>border （边框）</code>和周围元素实际所占空间的距离。</p><p>如果将一个元素的 margin 设置为负值，则元素将会变大。</p><h3 id="padding"><a href="#padding" class="headerlink" title="padding"></a>padding</h3><p>有时你将需要自定义一个元素，使它的每一个边具有不同的 <code>padding</code>。</p><p>CSS 允许你使用 <code>padding-top</code>、<code>padding-right</code>、<code>padding-bottom</code> 和 <code>padding-left</code>属性来控制元素四个方向的 <code>padding</code>。</p><p>还可以集中起来指定它们，如下所示：</p><p><code>padding: 10px 20px 10px 20px;</code></p><p>这四个值以顺时针方式排列：顶部、右侧、底部、左侧，简称：上右下左。</p><h3 id="margin"><a href="#margin" class="headerlink" title="margin"></a>margin</h3><p>将需要自定义一个元素，使它的每一个边具有不同的 <code>margin</code>。</p><p>CSS 允许你使用 <code>margin-top</code>、<code>margin-right</code>、<code>margin-bottom</code> 和 <code>margin-left</code> 属性来控制元素四个方向的<code>margin</code>。</p><p>还可以集中起来指定它们，如下所示：</p><p><code>margin: 10px 20px 10px 20px;</code></p><p>这四个值以顺时针方式排列：顶部、右侧、底部、左侧，简称：上右下左。</p><h1 id="CSS"><a href="#CSS" class="headerlink" title="CSS"></a>CSS</h1><h3 id="CSS继承Body样式元素"><a href="#CSS继承Body样式元素" class="headerlink" title="CSS继承Body样式元素"></a>CSS继承Body样式元素</h3><p>每一个 HTML 页面都有一个 body 元素。</p><p>我们可以证明body元素的存在，将其 background-color 设置为黑色。</p><pre><code>&lt;style&gt;     body{        background-color: black;        color: green;        font-family: Monospace;     }&lt;/style&gt;</code></pre><h3 id="CSS-样式的覆盖"><a href="#CSS-样式的覆盖" class="headerlink" title="CSS 样式的覆盖"></a>CSS 样式的覆盖</h3><p>HTML 元素会得到多个相互冲突的样式,<code>class</code> 会<code>override（覆盖）</code> <code>body</code>元素的<code>color</code></p><p>如下，<code>CSS class</code>的<code>pink</code>会覆盖<code>body</code>的<code>green</code></p><pre><code>&lt;style&gt;     body {        background-color: black;        font-family: Monospace;        color: green;    }    .pink-text{           color: pink;    }  &lt;/style&gt; &lt;h1 class=&quot;pink-text&quot;&gt;Hello World!&lt;/h1&gt;</code></pre></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;h1 id=&quot;标题-lt-h1-gt-lt-h1-gt&quot;&gt;&lt;a href=&quot;#标题-lt-h1-gt-lt-h1-gt&quot; class=&quot;headerlink&quot; title=&quot;标题 (&amp;lt;h1&amp;gt; &amp;lt;/h1&amp;gt;)&quot;&gt;&lt;/a&gt;标题 (&lt;code&gt;&amp;lt;h1&amp;gt; &amp;lt;/h1&amp;gt;&lt;/code&gt;)&lt;/h1&gt;&lt;p&gt;1.大多数HTML元素都有一个 开始标签 和一个 结束标签 。&lt;/p&gt;
&lt;p&gt;开始标签看起来像这样： &lt;code&gt;&amp;lt;h1&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;结束标签看起来像这样： &lt;code&gt;&amp;lt;/h1&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;h1 元素通常用于主标题，而 h2 元素通常用于副标题。还有 h3，h4，h5 和 h6 元素来表示不同的和新的部分。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;h1&amp;gt;Hello World&amp;lt;/h1&amp;gt;
&amp;lt;h2&amp;gt;CatPhotoApp&amp;lt;/h2&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
    
    </summary>
    
      <category term="Html" scheme="http://www.hfkstackoverflow.top/categories/Html/"/>
    
    
      <category term="Html" scheme="http://www.hfkstackoverflow.top/tags/Html/"/>
    
  </entry>
  
  <entry>
    <title>iOS界面调试工具Reveal4 配置</title>
    <link href="http://www.hfkstackoverflow.top/2018/10/25/iOS%E7%95%8C%E9%9D%A2%E8%B0%83%E8%AF%95%E5%B7%A5%E5%85%B7Reveal4-%E9%85%8D%E7%BD%AE/"/>
    <id>http://www.hfkstackoverflow.top/2018/10/25/iOS界面调试工具Reveal4-配置/</id>
    <published>2018-10-25T03:21:35.000Z</published>
    <updated>2019-04-09T14:37:02.049Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a><strong>简介</strong></h2><p><a href="https://revealapp.com/" target="_blank" rel="noopener">Reveal</a> 是一个界面调试工具。使用Reveal，我们可以在iOS开发时动态地查看和修改应用程序的界面。它类似Chrome的“审查元素”功能，我们不但可以在运行时看到iOS程序的界面层级关系，还可以实时地修改程序界面，不用重新运行程序就可以看到修改之后的效果。在使用时，我们将Reveal连接上模拟器或真机上正在运行的iOS程序，然后就可以查看和调试iOS程序的界面。<br><a id="more"></a>  </p><p></p><p><br><strong>Reveal效果图</strong>：</p><p>  <img src="/assets/blogImg/Reveal_Demo.jpg" alt="Reveal_Demo"></p><ol><li>左边部分是整个界面的层级关系，在这里可以以树形级层的方式来查看整个界面元素。</li><li>中间部分是一个可视化的查看区域，用户可以在这里切换2D或3D的查看方式，这里看到的也是程序运行的实时界面。</li><li>右边部边是控件的详细参数查看区域，当我们选中某一个具体的控件时，右边就可以显示出该控件的具体的参数列表。我们除了可以查看这些参数值是否正确外，还可以尝试修改这些值。所有的修改都可以实时反应到中间的实时预览区域。</li></ol><p>Reveal工具适合调试非Interface Builder创建的界面，Interface Builder中创建的xib和storyboard在企业开发中并不是总能胜任。</p><p>一方面的原因是企业开发常常是多人协作，xib和storyboard对于版本管理工具并不友好，例如xib和storyboard中有编辑者使用的操作系统和Xcode的版本号，这样一旦协同开发者的操作系统版本或Xcode版本不一致，则每次即使是打开xib和storyboard不做任何操作都会修改其内容。</p><p>另一方面是由于商业应用的界面复杂性，xib和storyboard并不能方便地提供更细粒度的模块复用。</p><p>使用Reveal可以很好地调试使用代码编写的UI界面。</p><h2 id="配置"><a href="#配置" class="headerlink" title="配置"></a><strong>配置</strong></h2><p>由于Reveal的升级，原来的1.X版本的配置已经不适合Reveal 4的配置了，原来的配置方法已经失效了，下面按照官网上介绍的三种方式进行相应的配置。  </p><p></p><p><br><a href="https://pan.baidu.com/s/1tYHQ8Sy9oqnmwyLY1QgT7g" target="_blank" rel="noopener">Reveal 4破解版( 密码:1ci8)</a></p><p></p><p></p><h3 id="1-方式一：使用CocoaPods"><a href="#1-方式一：使用CocoaPods" class="headerlink" title="1.方式一：使用CocoaPods"></a><strong>1.方式一：使用CocoaPods</strong></h3><p>如果你的项目使用了CocoaPods，那么只需要在<code>Podfile</code>添加相应版本即可。</p><p> <strong>1.</strong>我们查看<code>Reveal-SDK</code>的所有版本，通过终端搜索</p><p><pre><code>pod search Reveal-SDK</code></pre></p><p></p><p><br><img src="/assets/blogImg/Reveal_PodSearch.png" alt="Reveal_PodSearch"></p><p></p><p><br><strong>2.</strong>上面Reveal 4破解版使用的是14版本，所以在<code>Podfile</code>添加</p><p><pre><code>pod &apos;Reveal-SDK&apos;, &apos;~&gt; 14&apos;, :configurations =&gt; [&apos;Debug&apos;]</code></pre></p><p>注：<code>&#39;~&gt; 14&#39;</code>应该保持和你安装的Reveal版本号一致。 <code>:configurations</code>参数确保Reveal仅在到Debug中生效。</p><p></p><p><br><strong>3.</strong>然后只需在终端执行</p><pre><code>pod install 或者 pod update</code></pre><p><br></p><h3 id="2-方式二：使用Xcode-Breakpoint"><a href="#2-方式二：使用Xcode-Breakpoint" class="headerlink" title="2.方式二：使用Xcode Breakpoint"></a><strong>2.方式二：使用Xcode Breakpoint</strong></h3><p> <strong>1.</strong>打开Reveal,依次点击<code>Help -- Install Debugger Commands -- Continue -- Done</code></p><p> <img src="/assets/blogImg/Reveal_Breakpoint_1.png" alt="Reveal_Breakpoint_1"><br> </p><p><br> <img src="/assets/blogImg/Reveal_Breakpoint_2.png" alt="Reveal_Breakpoint_2"><br> </p><p><br> <strong>2.</strong>打开你的项目,依次点击<code>View -- Navigators -- Show Breakpoint Navigator</code>，或者直接点击断点图标。</p><p> <img src="/assets/blogImg/Reveal_Breakpoint_3.png" alt="Reveal_Breakpoint_3"><br>  </p><p><br> <strong>3.</strong>添加断点<code>“+” -- Symbolic Breakpoint…</code>。</p><p> <img src="/assets/blogImg/Reveal_Breakpoint_4.png" alt="Reveal_Breakpoint_4"><br> </p><p><br> <strong>4.</strong>设置断点()。</p><p> 1.点击断点选中<code>Edit Breakpoint…</code><br> 2.在<code>Symbol</code>中填入<code>UIApplicationMain</code><br> 3.点击<code>Add Action</code>，设置<code>Debugger Command</code>，填入如下</p><pre><code>expr (Class)NSClassFromString(@&quot;IBARevealLoader&quot;) == nil ? (void *)dlopen(&quot;/Applications/Reveal.app/Contents/SharedSupport/iOS-Libraries/RevealServer.framework/RevealServer&quot;, 0x2) : ((void*)0)                                        </code></pre><p> 4.勾中<code>Automatically continue after evaluating actions</code>。<br>  </p><p><br> <img src="/assets/blogImg/Reveal_Breakpoint_5.png" alt="Reveal_Breakpoint_5"></p><p> 5.再次点击断点选中<code>Move Breakpoint To</code>，选中<code>User</code><br>  </p><p><br> <img src="/assets/blogImg/Reveal_Breakpoint_6.png" alt="Reveal_Breakpoint_6"></p><p> 6.设置真机，选中<code>TARGETS -- Build Phases -- New Run Script Phase -- 命名&quot;Integrate Reveal Server&quot;</code>，加入以下shell script</p><pre><code>REVEAL_APP_PATH=$(mdfind kMDItemCFBundleIdentifier=&quot;com.ittybittyapps.Reveal2&quot; | head -n 1)BUILD_SCRIPT_PATH=&quot;${REVEAL_APP_PATH}/Contents/SharedSupport/Scripts/reveal_server_build_phase.sh&quot;if [ &quot;${REVEAL_APP_PATH}&quot; -a -e &quot;${BUILD_SCRIPT_PATH}&quot; ]; then    &quot;${BUILD_SCRIPT_PATH}&quot;else    echo &quot;Reveal Server not loaded: Cannot find a compatible Reveal app.&quot;fi</code></pre><p>  </p><p><br> <img src="/assets/blogImg/Reveal_Breakpoint_7.png" alt="Reveal_Breakpoint_7"><br>  </p><p><br> <img src="/assets/blogImg/Reveal_Breakpoint_8.png" alt="Reveal_Breakpoint_8"></p><p><br></p><h3 id="3-方式三：导入Reveal-framework"><a href="#3-方式三：导入Reveal-framework" class="headerlink" title="3.方式三：导入Reveal framework"></a><strong>3.方式三：导入Reveal framework</strong></h3><p> <strong>1.</strong>打开Reveal,依次点击<code>Help -- Show Reveal Library in Finder -- “iOS Library&quot; or &quot;tvOS Library”</code></p><p>  <img src="/assets/blogImg/Reveal_Breakpoint_9.png" alt="Reveal_Breakpoint_9"><br> </p><p><br> <strong>2.</strong>打开你的项目文件,将相应的framework拷进去。</p><p>  <img src="/assets/blogImg/Reveal_Breakpoint_10.png" alt="Reveal_Breakpoint_10"><br> </p><p><br> <strong>3.</strong>打开你的项目,<code>Build Settings -- Framework Search Paths -- “$(inherited) $(SRCROOT)“</code>。</p><p>  <img src="/assets/blogImg/Reveal_Breakpoint_11.png" alt="Reveal_Breakpoint_11"><br> </p><p><br> <strong>4.</strong><code>Build Settings -- Other Linker Flags -- “-ObjC -weak_framework RevealServer”</code>。</p><p>   <img src="/assets/blogImg/Reveal_Breakpoint_12.png" alt="Reveal_Breakpoint_12"><br> </p><p><br> <strong>5.</strong><code>Build Settings -- Runpath Search Paths -- “$(inherited) @executable_path/Frameworks”</code>。</p><p>   <img src="/assets/blogImg/Reveal_Breakpoint_13.png" alt="Reveal_Breakpoint_13"><br> </p><p><br> <strong>6.</strong><code>Build Phases -- Run Script</code>,加入下面<code>shell script</code>并且命名为<code>Integrate Reveal Server</code></p><pre><code> export REVEAL_SERVER_FILENAME=&quot;RevealServer.framework&quot;# Update this path to point to the location of RevealServer.framework in your project.export REVEAL_SERVER_PATH=&quot;${SRCROOT}/${REVEAL_SERVER_FILENAME}&quot;# If configuration is not Debug, skip this script.[ &quot;${CONFIGURATION}&quot; != &quot;Debug&quot; ] &amp;&amp; exit 0# If RevealServer.framework exists at the specified path, run code signing script.if [ -d &quot;${REVEAL_SERVER_PATH}&quot; ]; then  &quot;${REVEAL_SERVER_PATH}/Scripts/copy_and_codesign_revealserver.sh&quot;else  echo &quot;Reveal Server not loaded: RevealServer.framework could not be found.&quot;fi</code></pre><p>   <img src="/assets/blogImg/Reveal_Breakpoint_14.png" alt="Reveal_Breakpoint_14"></p><h3 id="4-结果：最终效果"><a href="#4-结果：最终效果" class="headerlink" title="4.结果：最终效果"></a><strong>4.结果：最终效果</strong></h3><p>   <img src="/assets/blogImg/Reveal_Breakpoint_15.png" alt="Reveal_Breakpoint_15"><br>    </p><p><br>    注：真机情况下，会出现授权，选中允许</p><p>   <img src="/assets/blogImg/Reveal_Breakpoint_16.png" alt="Reveal_Breakpoint_16"><br>    </p><p><br>   <img src="/assets/blogImg/Reveal_Breakpoint_17.png" alt="Reveal_Breakpoint_17"></p><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a><strong>总结</strong></h2><ol><li>Xcode 必须选择9.0或者以上的系统，如果运行不成功，检查一下Xcode配置。</li><li>配置方式一和三，上架时需要去除相关配置。</li></ol>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;&lt;strong&gt;简介&lt;/strong&gt;&lt;/h2&gt;&lt;p&gt;&lt;a href=&quot;https://revealapp.com/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Reveal&lt;/a&gt; 是一个界面调试工具。使用Reveal，我们可以在iOS开发时动态地查看和修改应用程序的界面。它类似Chrome的“审查元素”功能，我们不但可以在运行时看到iOS程序的界面层级关系，还可以实时地修改程序界面，不用重新运行程序就可以看到修改之后的效果。在使用时，我们将Reveal连接上模拟器或真机上正在运行的iOS程序，然后就可以查看和调试iOS程序的界面。&lt;br&gt;
    
    </summary>
    
      <category term="iOS" scheme="http://www.hfkstackoverflow.top/categories/iOS/"/>
    
    
      <category term="Objective-C" scheme="http://www.hfkstackoverflow.top/tags/Objective-C/"/>
    
      <category term="Tool" scheme="http://www.hfkstackoverflow.top/tags/Tool/"/>
    
      <category term="Reveal" scheme="http://www.hfkstackoverflow.top/tags/Reveal/"/>
    
  </entry>
  
  <entry>
    <title>JazzHands使用</title>
    <link href="http://www.hfkstackoverflow.top/2018/01/20/JazzHands%E4%BD%BF%E7%94%A8/"/>
    <id>http://www.hfkstackoverflow.top/2018/01/20/JazzHands使用/</id>
    <published>2018-01-20T02:04:11.000Z</published>
    <updated>2019-04-09T14:37:08.720Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a><strong>简介</strong></h2><blockquote><p>JazzHands是UIKit一个简单的关键帧基础动画框架。可以通过手势、scrollView、KVO或者RectiveCocoa控制动画从而实现一些交互式动画。常见用途：制作一些炫酷的引导页;</p><ul><li>下载地址及安装(可以通过CocoaPods安装)：</li></ul><p><a href="https://github.com/IFTTT/JazzHands" target="_blank" rel="noopener">OC版</a></p><pre><code>pod &quot;JazzHands&quot;</code></pre><p><a href="https://github.com/IFTTT/RazzleDazzle" target="_blank" rel="noopener">Swift版</a></p><pre><code>pod &quot;RazzleDazzle&quot;</code></pre></blockquote><a id="more"></a><h2 id="基础类"><a href="#基础类" class="headerlink" title="基础类"></a><strong>基础类</strong></h2><blockquote><ul><li><p><strong>IFTTTAnimatior</strong>：导演类。里面有一个管理所有动画的添加、删除、执行的数组(<code>IFTTTAnimation</code>)。</p></li><li><p><strong>IFTTTAnimatable</strong>：执行动画类，定义了核心协议(接口),<code>animate:(CGFloat)time</code>; 计算每一个时间点当前对象的值.每一种动画都要实现这个接口.如<code>IFTTTAlphaAnimation</code>类,计算一个时间点对应的对象的alpha值。</p></li><li><strong>IFTTTAnimation</strong>：动画基类,定义了一个对象的动画.仅仅只是对类中的<code>IFTTTFilemstrip</code>进行一个简单的封装。</li><li><strong>IFTTTFilmstrip</strong>：胶片类,有一个关键帧(IFTTTKeyframe)数组，添加/修改/获取对应关键帧(IFTTTKeyframe)的值。</li><li><strong>IFTTTKeyframe</strong>：关键帧类,描述每一个关键帧的时间对应的值。</li></ul></blockquote><h2 id="动画类型"><a href="#动画类型" class="headerlink" title="动画类型"></a><strong>动画类型</strong></h2><blockquote><ul><li><strong>IFTTTAlphaAnimation</strong>：<code>alpha</code>属性(实现淡入淡出效果)</li><li><strong>IFTTTRotationAnimation</strong>：旋转，通过改变view的<code>transform</code>从而实现旋转动画</li><li><strong>IFTTTBackgroundColorAnimation</strong>：背景色，<code>backgroundColor</code>属性.</li><li><strong>IFTTTCornerRadiusAnimation</strong>：圆角，改变视图的<code>layer.cornerRadius</code>属性</li><li><strong>IFTTTHideAnimation</strong>：显示隐藏，改变view的显示及隐藏，<code>view.hidden</code></li><li><strong>IFTTTScaleAnimation</strong>：缩放变换，修改<code>view.transform</code>属性</li><li><strong>IFTTTTranslationAnimation</strong>：平移变换，改变<code>view.transform</code>属性</li><li><strong>IFTTTTransform3DAnimation</strong>：<code>layer.transform</code>属性 (是3D变换)。</li><li><strong>IFTTTTextColorAnimation</strong>：UILabel的<code>textColor</code>属性，修改Label的textColor</li><li><strong>IFTTTFillColorAnimation</strong>：CAShapeLayer的<code>fillColor</code>属性</li><li><strong>IFTTTStrokeStartAnimation</strong>：CAShapeLayer的<code>strokeStart</code>属性</li><li><strong>IFTTTStrokeEndAnimation</strong>：CAShapeLayer的<code>strokeEnd</code>属性</li><li><strong>IFTTTPathPositionAnimation</strong>：UIView的<code>layer.position</code>属性</li><li><strong>IFTTTConstraintConstantAnimation</strong>：修改AutoLayout Constraint的<code>constant</code>属性</li><li><strong>IFTTTConstraintMultiplierAnimation</strong>：修改 AutoLayout<code>Multiplier</code>属性</li><li><strong>IFTTTScrollViewPageConstraintAnimation</strong>：用 AutoLayout 修改 ScrollView 和 View 的位置关系</li><li><strong>IFTTTFrameAnimation</strong>：修改 View 的<code>frame</code>属性</li></ul></blockquote><h2 id="应用一"><a href="#应用一" class="headerlink" title="应用一"></a><strong>应用一</strong></h2><p>&gt;</p><blockquote><p>通过其中一个类(<code>IFTTTAlphaAnimation</code>)了解JazzHands,</p><p>   <img src="/assets/blogImg/JazzHands_Demo.gif" alt="JazzHands_Demo"></p><pre><code>@interface AlphaViewController ()&lt;UIScrollViewDelegate&gt;@property (strong, nonatomic) UIScrollView *scrollView;@property (strong,nonatomic) UIView *alphaView;@property (strong,nonatomic) IFTTTAnimator *animator;@end@implementation AlphaViewController- (void)viewDidLoad {    [super viewDidLoad];    // Do any additional setup after loading the view.    self.view.backgroundColor=[UIColor whiteColor];    self.animator=[[IFTTTAnimator alloc]init];    self.scrollView=[[UIScrollView alloc]initWithFrame:self.view.bounds];    self.scrollView.pagingEnabled=YES;    self.scrollView.backgroundColor=[UIColor whiteColor];    self.scrollView.contentSize=CGSizeMake(self.view.frame.size.width*2,0);    self.scrollView.delegate=self;    [self.view addSubview:self.scrollView];    self.alphaView=[[UIView alloc]initWithFrame:CGRectMake(300,200, 40, 40)];    self.alphaView.backgroundColor=[UIColor blackColor];    [self.scrollView addSubview:self.alphaView];    [self setupAlphaAnimation];}-(void)setupAlphaAnimation{    IFTTTAlphaAnimation *alpha=[IFTTTAlphaAnimation animationWithView:self.alphaView];    [self.animator addAnimation:alpha];    [alpha addKeyframeForTime:0 alpha:1.0];    [alpha addKeyframeForTime:200 alpha:0.0];}-(void)scrollViewDidScroll:(UIScrollView *)scrollView{    [self.animator animate:scrollView.contentOffset.x];}- (void)didReceiveMemoryWarning {    [super didReceiveMemoryWarning];    // Dispose of any resources that can be recreated.}@end</code></pre><p>   <strong>1.实现效果:中间黑色方块随着scrollView滚动透明度逐渐变为0;</strong></p><ol><li>创建一个scrollView并且在上面添加一个黑色方块.</li><li>创建<code>IFTTTAlphaAnimation</code>并且添加到<code>IFTTTAnimator</code>中,同时添加两个关键帧0 - 1.0 和 200 - 0.0. </li><li>再在scrollView的代理方法中调用<code>IFTTTAnimator</code>的<code>animate:</code>方法,从而实现相应透明度变化的动画.</li></ol><p><strong>2.IFTTTAlphaAnimation</strong></p><p><code>IFTTTAlphaAnimation : IFTTTViewAnimation &lt;IFTTTAnimatable&gt;</code></p><p><code>IFTTTAlphaAnimation</code>继承于动画父类<code>IFTTTViewAnimation</code>,实现<code>IFTTTAnimatable</code>protocal,而 IFTTTAnimatable 只有一个方法:</p><pre><code>@protocol IFTTTAnimatable &lt;NSObject&gt;- (void)animate:(CGFloat)time;@end</code></pre><p>所有其他类型的动画都继承自这个类 IFTTTAnimation</p><pre><code>@interface IFTTTAnimation : NSObject- (void)addKeyframeForTime:(CGFloat)time value:(id&lt;IFTTTInterpolatable&gt;)value;- (void)addKeyframeForTime:(CGFloat)time value:(id&lt;IFTTTInterpolatable&gt;)value withEasingFunction:(IFTTTEasingFunction)easingFunction;- (id&lt;IFTTTInterpolatable&gt;)valueAtTime:(CGFloat)time;- (BOOL)hasKeyframes;@end</code></pre><p>类扩展中有如下属性:</p><pre><code>@interface IFTTTAnimation ()@property (nonatomic, strong) IFTTTFilmstrip *filmstrip;@end</code></pre><ol><li><p>动画通过调用[addKeyframeForTime: value: ]来添加动画相应的关键帧,通过父类[self.filmstrip setValue: atTime:]方法进行存储关键帧,每个动画的关键帧都是单独的一个<code>IFTTTKeyframe</code>对象,它包括设置的时间点以及相应时间点对应的值.</p></li><li><p>IFTTTFilmstrip 类中有一个数组,存储每一个添加的 IFTTTKeyframe 对象, 这个类负责计算在任意时间点时,我们的帧动画对应的值.</p></li></ol><p><strong>3.IFTTTAnimator</strong></p><pre><code>@interface IFTTTAnimator : NSObject- (void)addAnimation:(id&lt;IFTTTAnimatable&gt;)animation;- (void)removeAnimation:(id&lt;IFTTTAnimatable&gt;)animation;- (void)removeAllAnimations;- (void)animate:(CGFloat)time;@end</code></pre><p>类扩展中如下:</p><pre><code>@interface IFTTTAnimator ()@property (nonatomic, strong) NSMutableArray *animations;@end</code></pre><p><code>IFTTTAnimator</code>通过animations保存我们添加的animation,然后在 scrollView 滚动时,触发 <code>- (void)animate:(CGFloat)time</code>;它内部的实现非常简单:</p><pre><code>- (void)animate:(CGFloat)time{    for (id&lt;IFTTTAnimatable&gt; animation in self.animations) {        [animation animate:time];    }}</code></pre><p>动画效果的产生,每个动画是如何和应用到 View 上的</p><p><code>IFTTTAlphaAnimation</code> 中的<code>- (void)animate:(CGFloat)time;</code>方法</p><pre><code>- (void)animate:(CGFloat)time{    if (!self.hasKeyframes) return;////没有设置动画帧,就返回    self.view.alpha = (CGFloat)[(NSNumber *)[self valueAtTime:time] floatValue];// //如果设置了动画帧,计算当前时间点时的透明度,设置给 View }</code></pre><p>然后调用</p><pre><code>- (id&lt;IFTTTInterpolatable&gt;)valueAtTime:(CGFloat)time{    if (self.filmstrip.isEmpty) return nil;    return [self.filmstrip valueAtTime:time];}</code></pre><p>可见 fileStrip 是进行计算任务的.</p><p>IFTTTFilmstrip 的计算过程如下:</p><pre><code>- (id&lt;IFTTTInterpolatable&gt;)valueAtTime:(CGFloat)time{    NSAssert(!self.isEmpty, @&quot;At least one KeyFrame must be set before animation begins.&quot;);    id value;    NSUInteger indexAfter = [self indexOfKeyframeAfterTime:time];//当前时间点对应哪一帧动画    if (indexAfter == 0) {        //如果时间点没到达第一帧动画发生的时间,就让值为第一帧的值.        value = ((IFTTTKeyframe *)self.keyframes[0]).value;    } else if (indexAfter &lt; self.keyframes.count) {        //如果时间点在2帧之间,取出这2帧        IFTTTKeyframe *keyframeBefore = (IFTTTKeyframe *)self.keyframes[indexAfter - 1];        IFTTTKeyframe *keyframeAfter = (IFTTTKeyframe *)self.keyframes[indexAfter];        //根据时间计算现在的动画进行到 这2帧之间的百分比         CGFloat progress = [self progressFromTime:keyframeBefore.time toTime:keyframeAfter.time atTime:time withEasingFunction:keyframeBefore.easingFunction];        //根据百分比算出当前时间点对应的值        value = [keyframeBefore.value interpolateTo:keyframeAfter.value withProgress:progress];    } else {         //如果时间超过最后一帧动画发生的时间,就让值为最后一帧的值.        value = ((IFTTTKeyframe *)self.keyframes.lastObject).value;    }    return value;}</code></pre><p>上面Demo中:</p><ul><li><p><strong>0-1.0</strong> : scrollView滚动 0 px 时,没到达发生动画的时间,view保持第一帧的值,即 Alpha = 1.0</p></li><li><p><strong>200 - 0.0</strong> : scrollView 滚动scrollview.contentOffset &gt; 200 px 时,时间超过最后一帧动画发生的时间, Alpha 保持 0.0</p></li><li><p><strong>在 0 - 200 之间(例如 40)</strong> : 首先取出离当前时间点之前和之后的2帧,然后<code>[progressFromTime: toTime: atTime:withEasingFunction:]</code>方法计算当前进行到这2帧之间的百分比.</p></li></ul><pre><code>- (CGFloat)progressFromTime:(CGFloat)fromTime toTime:(CGFloat)toTime atTime:(CGFloat)atTime withEasingFunction:(IFTTTEasingFunction)easingFunction{    CGFloat duration = toTime - fromTime;// duration = 200 - 0 = 200;     if (duration == 0.f) return 0.f;    CGFloat timeElapsed = atTime - fromTime; // timeElapsed = 40 - 0 =40    // 当前时间 / 这2帧的时间差 得出 &quot;进行到这2帧之间的百分比&quot;    return easingFunction(timeElapsed / duration);// return (40/200) = 0.2}</code></pre><p>最后根据 “进行到这2帧之间的百分比” 计算:</p><pre><code> + (CGFloat)interpolateCGFloatFrom:(CGFloat)fromValue to:(CGFloat)toValue withProgress:(CGFloat)progress{     CGFloat totalChange = toValue - fromValue; //totalChange = 0- 1 = -1;     CGFloat currentChange = totalChange * progress; //currentChange= -1 * 0.2 = -0.2;     return fromValue + currentChange;// return 1.0 + (-0.2) = 0.8;}</code></pre><p> 当前时间点对应的值,当前时间点 40px 时,对应的View 的 Alpha 值为0.8,将其赋值给 View self.view.alpha = 0.8,这样就产生根据 scrollView 滚动距离,改变 View Alpha 的帧动画.</p><p><strong>4. IFTTTEasingFunction</strong></p><p><code>[UIView animateWithDuration: delay: options: animations: completion:]</code> 方法中,option 可以设置为:</p><pre><code>typedef NS_ENUM(NSInteger, UIViewAnimationCurve) {        UIViewAnimationCurveEaseInOut,  //先慢慢加速,然后匀速,最后慢慢减速,符合现实运动规律        UIViewAnimationCurveEaseIn,     // 先慢后快,慢慢加速        UIViewAnimationCurveEaseOut,    // 先快后满,慢慢减速        UIViewAnimationCurveLinear        // 匀速    };</code></pre><p>同样 CAMediaTimingFunction(速度控制函数)也有(如果想研究这些数学公式或者实现具体实现):</p><p>可以参照<a href="https://github.com/AttackOnDobby/iOS-Core-Animation-Advanced-Techniques/blob/master/10-%E7%BC%93%E5%86%B2/%E7%BC%93%E5%86%B2.md" target="_blank" rel="noopener">iOS-Core-Animation-Advanced-Techniques</a></p><pre><code>NSString * const kCAMediaTimingFunctionLinear;//(线性):匀速,给你一个相对静态的感觉NSString * const kCAMediaTimingFunctionEaseIn;//(渐进):动画缓慢进入,然后加速离开NSString * const kCAMediaTimingFunctionEaseOut;//(渐出):动画全速进入,然后减速的到达目的地NSString * const kCAMediaTimingFunctionEaseInEaseOut;//(渐进渐出):动画缓慢的进入,中间加速,然后减速的到达目的地。这个是默认的动画行为NSString * const kCAMediaTimingFunctionDefault;//效果最接近 kCAMediaTimingFunctionEaseInEaseOut</code></pre><p>在 JazzHands 中有这些:</p><pre><code>typedef CGFloat (^IFTTTEasingFunction) (CGFloat t);UIKIT_EXTERN IFTTTEasingFunction const IFTTTEasingFunctionLinear;UIKIT_EXTERN IFTTTEasingFunction const IFTTTEasingFunctionEaseInQuad;UIKIT_EXTERN IFTTTEasingFunction const IFTTTEasingFunctionEaseOutQuad;UIKIT_EXTERN IFTTTEasingFunction const IFTTTEasingFunctionEaseInOutQuad;UIKIT_EXTERN IFTTTEasingFunction const IFTTTEasingFunctionEaseInCubic;UIKIT_EXTERN IFTTTEasingFunction const IFTTTEasingFunctionEaseOutCubic;UIKIT_EXTERN IFTTTEasingFunction const IFTTTEasingFunctionEaseInOutCubic;UIKIT_EXTERN IFTTTEasingFunction const IFTTTEasingFunctionEaseInBounce; //Bounce 小球自由落体到地面并反复弹起的效果UIKIT_EXTERN IFTTTEasingFunction const IFTTTEasingFunctionEaseOutBounce;</code></pre><p><strong>CAMediaTimingFunction 产生的效果如下:</strong></p><p>   <img src="/assets/blogImg/JazzHands_CAMediaTimingFunction.png" alt="CAMediaTimingFunction效果图"></p><p><strong>5. IFTTTInterpolatable</strong></p><p><code>IFTTTInterpolatable</code> 是一个 protocal :</p><pre><code>@protocol IFTTTInterpolatable &lt;NSObject&gt;- (id)interpolateTo:(id)toValue withProgress:(CGFloat)progress;@end</code></pre><p>它是用来计算当前时间点对应的值的.<br>上面的例子中,我们的 scrollView 移动40px,progress 为 0.2, fromValue = 1.0, toValue = 0.0</p><pre><code>+ (CGFloat)interpolateCGFloatFrom:(CGFloat)fromValue to:(CGFloat)toValue withProgress:(CGFloat)progress{    CGFloat totalChange = toValue - fromValue; // totalChange = 0 - 1 = -1;    CGFloat currentChange = totalChange * progress; // currentChange= -1 * 0.2 = -0.2;    return fromValue + currentChange;        // return 1.0 + (-0.2) = 0.8;}</code></pre><p>计算的出我们的 view.alpha = 0.8</p><p>其他的动画 比如 Translation ,Scale ,ConstraintConstant ,CornerRadius 的值都是 CGFloat,只不过经过上面的计算之后赋值给不同的属性而已,</p><p>在他们各自的 <code>- (void)animate:(CGFloat)time</code> 方法中</p><pre><code>self.constraint.constant = [self valueAtTime:time]; //IFTTTConstraintConstantAnimationself.view.layer.cornerRadius = [self valueAtTime:time]; //IFTTTCornerRadiusAnimation</code></pre><p>Translation 和 Scale Rotation 动画都是修改 view.transform 属性</p><pre><code>CGFloat scale = (CGFloat)[(NSNumber *)[self valueAtTime:time] floatValue];CGAffineTransform scaleTransform = CGAffineTransformMakeScale(scale, scale);self.view.iftttScaleTransform = [NSValue valueWithCGAffineTransform:scaleTransform];CGAffineTransform newTransform = scaleTransform;if (self.view.iftttRotationTransform) {    newTransform = CGAffineTransformConcat(newTransform, [self.view.iftttRotationTransform CGAffineTransformValue]);}if (self.view.iftttTranslationTransform) {    newTransform = CGAffineTransformConcat(newTransform, [self.view.iftttTranslationTransform CGAffineTransformValue]);}self.view.transform = newTransform;</code></pre><p>先计算对应时间点的值,然后将所有 transform 用 CGAffineTransformConcat 拼接起来 ,赋值给 View</p><p><strong>6.其他 IFTTTInterpolatable</strong></p><p>如果我们要做改变 View 背景色或者 Frame 的动画呢,计算方式就不同了.<br>计算任意时间点对应的 Color:</p><pre><code>- (UIColor *)interpolateTo:(UIColor *)toValue withProgress:(CGFloat)progress{    CGFloat startRed, startBlue, startGreen, startAlpha;    CGFloat endRed, endBlue, endGreen, endAlpha;    UIColor *interpolatedColor = self;    if ([self.class iftttGetRed:&amp;startRed green:&amp;startGreen blue:&amp;startBlue alpha:&amp;startAlpha fromColor:self] &amp;&amp;        [self.class iftttGetRed:&amp;endRed green:&amp;endGreen blue:&amp;endBlue alpha:&amp;endAlpha fromColor:toValue]) {        CGFloat red = [NSNumber interpolateCGFloatFrom:startRed to:endRed withProgress:progress];        CGFloat green = [NSNumber interpolateCGFloatFrom:startGreen to:endGreen withProgress:progress];        CGFloat blue = [NSNumber interpolateCGFloatFrom:startBlue to:endBlue withProgress:progress];        CGFloat alpha = [NSNumber interpolateCGFloatFrom:startAlpha to:endAlpha withProgress:progress];        interpolatedColor = [UIColor colorWithRed:red green:green blue:blue alpha:alpha];    }    return interpolatedColor;}</code></pre><p>我们先把 from 和 to Color 的 R,G,B,A 分别取出来,然后作为 CGFloat 值分别计算,最后拼接成 UIColor</p><p>Frame 的计算:</p><pre><code>+ (CGRect)interpolateCGRectFrom:(CGRect)fromValue to:(CGRect)toValue withProgress:(CGFloat)progress{    CGPoint originBetween = [self interpolateCGPointFrom:fromValue.origin to:toValue.origin withProgress:progress];    CGSize sizeBetween = [self interpolateCGSizeFrom:fromValue.size to:toValue.size withProgress:progress];    return CGRectMake(originBetween.x, originBetween.y, sizeBetween.width, sizeBetween.height);}</code></pre><p>也很好理解,拆出其 origin 和 size 分别计算</p><p>origin 含有 x, y ,也就是2个 CGFloat 计算</p><p>size 含有 height 和 width 也是 2个 CGFloat</p><p>最后将所有计算完的 CGFloat 值拼成 CGRect 作为 当前时间的 Frame 值返回</p></blockquote><h2 id="应用二"><a href="#应用二" class="headerlink" title="应用二"></a><strong>应用二</strong></h2><blockquote><p>JazzHands主要应用于一些动画引导页,主要用<code>scrollviews</code>,JazzHands封装了<code>IFTTTAnimatedPagingScrollViewController</code>更加方便使用了，JazzHands框架是基于关键帧的动画，其概念类似于<code>Core Animation</code>，但是JazzHands动画驱动主要是坐标驱动。JazzHands中以<code>scrollview.contentOffset</code>作为动画的time。</p><p>   <img src="/assets/blogImg/JazzHands_Time.png" alt="time原理"></p><p>然后在<code>[scrollview didScroll]</code>代理中来进行计算相关视图的位置。<br>在JazzHands中</p><pre><code>- (void)keepView:(UIView *)view onPages:(NSArray *)pages atTimes:(NSArray *)times withAttribute:(IFTTTHorizontalPositionAttribute)attribute;</code></pre></blockquote><p>&gt;</p><blockquote><p>view表示需要加动画的视图</p><p>pages、offsets和attribute控制动画的显示。</p><p>其中pages和offsets的数量必须相同，否则会产生<code>crash</code></p><p>pages、offsets控制动画：</p><pre><code>view.frame.size.x = page * pageWidthscrollview.contentOffset = time * pageWidth</code></pre><p>例如：</p><pre><code>[self keepView:self.iftttPresents onPages:@[@(0), @(-1)] atTimes:@[@(0), @(1)]];</code></pre><p>当time=0时，即scrollview在初始位置，这时scrollview.contentOffset=(pageWidth <em> 1)。当time=1时，即scrollview移动到下个scrollview.contentOffset，这时scrollview.contentOffset=(pageWidth </em> 1)；相当于scrollview滚动到第二页时，我们必须设置时间time所对应的view的位置，view对象进行时间0到1的动画。每一个动画(animation)都拥有一个胶卷(filmstrip),每一个胶卷都包含该了该动作的所有关键帧(keyframe)。随着屏幕拖拽，JazzHands会根据约束和时间(contentOffset)计算对象的位置.只要刷新的频率只够高。我们人眼就看不出是重新画上去，而是连续的动画了。</p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;&lt;strong&gt;简介&lt;/strong&gt;&lt;/h2&gt;&lt;blockquote&gt;
&lt;p&gt;JazzHands是UIKit一个简单的关键帧基础动画框架。可以通过手势、scrollView、KVO或者RectiveCocoa控制动画从而实现一些交互式动画。常见用途：制作一些炫酷的引导页;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;下载地址及安装(可以通过CocoaPods安装)：&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;a href=&quot;https://github.com/IFTTT/JazzHands&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;OC版&lt;/a&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;pod &amp;quot;JazzHands&amp;quot;
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;&lt;a href=&quot;https://github.com/IFTTT/RazzleDazzle&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Swift版&lt;/a&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;pod &amp;quot;RazzleDazzle&amp;quot;
&lt;/code&gt;&lt;/pre&gt;&lt;/blockquote&gt;
    
    </summary>
    
      <category term="iOS" scheme="http://www.hfkstackoverflow.top/categories/iOS/"/>
    
    
      <category term="Objective-C" scheme="http://www.hfkstackoverflow.top/tags/Objective-C/"/>
    
      <category term="thirdpart" scheme="http://www.hfkstackoverflow.top/tags/thirdpart/"/>
    
  </entry>
  
  <entry>
    <title>HEXO+Github+Next搭建博客</title>
    <link href="http://www.hfkstackoverflow.top/2017/12/10/HEXO%E6%90%AD%E5%BB%BA%E4%B8%AA%E4%BA%BA%E5%8D%9A%E5%AE%A2/"/>
    <id>http://www.hfkstackoverflow.top/2017/12/10/HEXO搭建个人博客/</id>
    <published>2017-12-10T12:35:36.000Z</published>
    <updated>2019-04-10T07:02:07.824Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a><strong>前言</strong></h2><p>通过一个方式来整理和管理自己的知识，之前了解到可以通过 GitHub 托管静态网页，使用 Hexo 和 GitHub Pages 方便快捷地搭建个人博客网站，所以通过学习从零开始搭建、配置一个属于自己的博客网站。</p><a id="more"></a><h2 id="注册-GitHub"><a href="#注册-GitHub" class="headerlink" title="注册 GitHub"></a><strong>注册 GitHub</strong></h2><ol><li><p><a href="https://github.com/" target="_blank" rel="noopener">GitHub</a> 是一个面向开源及私有软件项目的托管平台。首先需要注册一个账号并且创建仓库，作用是用来做博客的远程创库、域名、服务器之类的。</p><blockquote><p>1.注意点：建立Repository，建立与你用户名对应的仓库，仓库名必须为[xxx.github.io]，固定写法。</p></blockquote><p><img src="/assets/blogImg/hexo_blog_githubNewR.png" alt="New_Repository"></p><p><img src="/assets/blogImg/hexo_blog_githubSSH.png" alt="GitHub_SSH"></p></li><li><p>配置SSH Keys，可以配置也也可以不配置。不配置的话以后每次对自己的博客有改动提交的时候就要手动输入账号密码，配置了每次访问就无需用户名和密码连接 GitHub 。<br>#####检查是否已经存在SSH Keys</p><blockquote><p>  打开终端，<code>ls -al ~/.ssh</code>查看结果，如果在.ssh目录下存在文件列表，说明已经存在 SSH keys 。</p></blockquote><p><img src="/assets/blogImg/hexo_blog_githubCheckSSH.png" alt="GitHub_SSHCheck"></p></li></ol><p>如果没有 public key ，生成 SSH；如果已经有了，直接配置即可。<br>输入<code>ssh-keygen -t rsa -b 4096 -C &quot;your_email@example.com&quot;</code><br>(将邮箱替换为你自己的地址)。然后一直回车下去，直到生成SSH Keys。</p><p>   <img src="/assets/blogImg/hexo_blog_githubCreateSSH.png" alt="GitHub_SSHCreate"></p><p> 生成SSH Keys后，到GitHub中去配置SSH Keys</p><pre><code>1. 找到`id_rsa.pub`中的`public key`,然后到GitHub Setting页面配置SSH。</code></pre><p>   <img src="/assets/blogImg/hexo_blog_githubNEWSSHK.png" alt="GitHub_SSHCreate"></p><p> 测试连接<br> 输入<code>ssh -T git@github.com</code></p><pre><code>可能看到警告：</code></pre><p> <img src="/assets/blogImg/hexo_blog_github_1.png" alt="hexo_blog_github_1"></p><pre><code>或</code></pre><p> <img src="/assets/blogImg/hexo_blog_github_2.png" alt="hexo_blog_github_2"></p><p> 验证 <code>fingerprint</code> ,然后 <code>type yes</code></p><p> 然后，</p><p>  <img src="/assets/blogImg/hexo_blog_github_3.png" alt="hexo_blog_github_3"></p><h2 id="正式安装Hexo"><a href="#正式安装Hexo" class="headerlink" title="正式安装Hexo"></a><strong>正式安装Hexo</strong></h2><p>Hexo是一款基于Node.js的静态博客框架，安装Hexo相当简单，可以参考<a href="https://hexo.io/zh-cn/docs/index.html" target="_blank" rel="noopener">官方文档</a>。</p><p><code>Node</code>和<code>Git</code>都安装好后，可执行如下命令安装<code>Hexo</code>：</p><pre><code>$ npm install -g hexo-cli</code></pre><p>如果没有安装<code>Node</code>和<code>Git</code>，以下均为 Mac 环境下。</p><pre><code># 使用 brew 安装 gitbrew install git# 可以使用 cURL 安装 nvm$ curl https://raw.github.com/creationix/nvm/master/install.sh | sh# 也可以 使用 Wget 安装 nvm$ wget -qO- https://raw.github.com/creationix/nvm/master/install.sh | sh# 使用最佳安装方式：nvm 安装 Node.js$ nvm install stable</code></pre><h3 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h3><p>创建一个文件夹，如：<code>Blog</code>，<code>cd</code>到<code>Blog</code>里执行hexo init的。命令：</p><pre><code>hexo init</code></pre><h3 id="生成静态页面"><a href="#生成静态页面" class="headerlink" title="生成静态页面"></a>生成静态页面</h3><p>继续再Blog目录下执行如下命令，生成静态页面</p><pre><code>hexo generate （hexo g  也可以）   </code></pre><h3 id="本地启动"><a href="#本地启动" class="headerlink" title="本地启动"></a>本地启动</h3><p>启动本地服务，进行文章预览调试，命令：</p><pre><code>hexo server （hexo s  也可以）  </code></pre><h3 id="访问"><a href="#访问" class="headerlink" title="访问"></a>访问</h3><p>访问 <a href="http://localhost:4000/" target="_blank" rel="noopener">本地配置</a>,本地已经简单的设置好了，但是现在域名和服务器都是基于自己的电脑，接下来需要跟github进行关联。</p><h2 id="配置Github"><a href="#配置Github" class="headerlink" title="配置Github"></a><strong>配置Github</strong></h2><h3 id="建立Repository"><a href="#建立Repository" class="headerlink" title="建立Repository"></a>建立Repository</h3><p>建立与你用户名对应的仓库，仓库名必须为<code>your_user_name.github.io</code>，固定写法 然后建立关联，我的Blog在本地/Users/leopard/Blog，Blog是我之前建的东西也全在这里面，有：</p><pre><code>Blog 　｜ 　｜－－ _config.yml 　｜－－ node_modules 　｜－－ public 　｜－－ source 　｜－－ db.json    　｜－－ package.json 　｜－－ scaffolds 　｜－－ themes 　</code></pre><p>现在我们需要_config.yml文件，来建立关联，命令：</p><pre><code>vim _config.yml</code></pre><p>翻到最下面，改成我这样子的，注意： <code>: 后面要有空格</code></p><p>配置更目录下的 _config.yml文件</p><pre><code>deploy: type: git repo: https://github.com/huangfangkai/huangfangkai.github.io.git branch: master</code></pre><p>部署到 GitHub，前提是已经注册 <code>GitHub</code>，创建 <code>username.github.io</code> 仓库<br>安装 <code>GitHub</code> 依赖插件</p><pre><code>$ npm install hexo-deployer-git --save</code></pre><h3 id="部署命令"><a href="#部署命令" class="headerlink" title="部署命令"></a>部署命令</h3><pre><code># 部署，hexo deploy$ hexo d$ hexo d -g</code></pre><p>访问个人主页<br>访问 <code>username.github.io</code></p><h2 id="部署步骤"><a href="#部署步骤" class="headerlink" title="部署步骤"></a><strong>部署步骤</strong></h2><p>每次部署的步骤，可按以下三步来进行。</p><pre><code>hexo cleanhexo generate (hexo g)hexo deploy (hexo d)</code></pre><p>一些常用命令：</p><pre><code>hexo new &quot;postName&quot; #新建文章hexo new page &quot;pageName&quot; #新建页面hexo generate #生成静态页面至public目录hexo server #开启预览访问端口（默认端口4000，&apos;ctrl + c&apos;关闭server）hexo deploy #将.deploy目录部署到GitHubhexo help  #查看帮助hexo version  #查看Hexo的版本</code></pre><p><strong>注意：</strong>这里需要注意的地方是：在执行 <code>hexo g</code> 命令时候，可能会出现以下 <code>error</code>,应该是 <code>yarn</code> 和 <code>npm</code> 冲突的依赖的问题，所以前面文章建议使用 <code>yarn</code> 管理包依赖</p><pre><code>ERROR Plugin load failed: hexo-renderer-markedError: EISDIR: illegal operation on a directory, readERROR Plugin load failed: hexo-serverError: EISDIR: illegal operation on a directory, readERROR Plugin load failed: hexo-renderer-stylusError: EISDIR: illegal operation on a directory, read</code></pre><p><strong>解决办法：</strong>先用 yarn 分别移除，再重新安装，如下</p><pre><code>## 先移除yarn remove hexo-renderer-markedyarn remove hexo-renderer-stylusyarn remove hexo-server## 再安装yarn add hexo-renderer-marked -S yarn add hexo-renderer-stylus -Syarn add hexo-server -S</code></pre><p>在执行 <code>hexo g</code> ,顺利执行 </p><h2 id="绑定域名"><a href="#绑定域名" class="headerlink" title="绑定域名"></a><strong>绑定域名</strong></h2><p>开始绑定自己的 域名 <a href="http://hfkstackoverflow.top" target="_blank" rel="noopener">hfkstackoverflow.top</a>，<br>我们绑定<code>域名</code>之后，只支持 <code>http</code> 协议，这样我们在 <code>Google</code> 浏览器访问时，会有<code>不安全标准</code>，而且，很容易被插入广告，于是搞个 <code>https</code>，采用第三方服务 <a href="https://app.netlify.com" target="_blank" rel="noopener">Netlify</a> 来提供免费的SSL/TLS证书。</p><h3 id="HTTP-绑定域名"><a href="#HTTP-绑定域名" class="headerlink" title="HTTP 绑定域名"></a>HTTP 绑定域名</h3><p>设置域名解析规则</p><p>首先，我们需要去运行商，新增域名解析规则，如图</p><p><img src="/assets/blogImg/hexo_blog_yumingsetting.png" alt="域名解析"></p><h3 id="新增-CNAME-文件"><a href="#新增-CNAME-文件" class="headerlink" title="新增 CNAME 文件"></a>新增 <code>CNAME</code> 文件</h3><p>配置好域名解析规则之后，还需要在项目的 .\source\ 路径下新增 CNAME 文件，输入内容 你的域名 ,如图</p><p><img src="/assets/blogImg/hexo_blog_CNAME.png" alt="CNAME"></p><p>重新新生成文件，推送到<code>GitHub</code></p><h3 id="HTTPS-绑定域名"><a href="#HTTPS-绑定域名" class="headerlink" title="HTTPS 绑定域名"></a><code>HTTPS</code> 绑定域名</h3><p>注册 <code>Netlify</code> 账号<br>首先，我们需要注册一个 <code>Netlify</code> 账号，可以选择用 <code>GitHub</code> 注册。</p><p><img src="/assets/blogImg/hexo_blog_Netlify.png" alt="Netlify"></p><h3 id="新增站点"><a href="#新增站点" class="headerlink" title="新增站点"></a>新增站点</h3><p>点击如图按钮 <code>New site from Git</code>,来新增 站点</p><p><img src="/assets/blogImg/hexo_blog_NetlifyNewSite1.png" alt="Netlify"></p><p>然后，点击如图按钮 <code>GitHub</code></p><p><img src="/assets/blogImg/hexo_blog_NetlifyNewSite2.png" alt="Netlify"></p><p>选择你的项目，比如我的 <code>huangfangkai.github.io</code></p><p>点击 <code>Deploy site</code></p><p>看到如图页面，表示成功，再点击 <code>Site settings</code></p><p><img src="/assets/blogImg/hexo_blog_NetlifyNewSite3.png" alt="Netlify"></p><p>再点击 <code>Change site name</code> 修改短名称，方便记忆</p><p><img src="/assets/blogImg/hexo_blog_NetlifyNewSite4.png" alt="Netlify"></p><p>在自己的域名管理中设置 <code>DNS</code> ，我使用的是 <code>阿里云</code> ，在管理界面添加如图规则</p><p><img src="/assets/blogImg/hexo_blog_NetlifyNewSite5.png" alt="Netlify"></p><p>在点击 <code>Domain management</code> 下的 <code>HTTPS</code> ，按照步骤完成操作，稍等片刻，即可看到我们的 域名 已经上锁</p><h2 id="主题"><a href="#主题" class="headerlink" title="主题"></a><strong>主题</strong></h2><p>这里有大量的<a href="https://github.com/hexojs/hexo/wiki/Themes" target="_blank" rel="noopener">主题列表</a>使用方法里面 都有详细的介绍，主要讲<a href="https://github.com/iissnan/hexo-theme-next" target="_blank" rel="noopener">Next</a>的配置。</p><h3 id="主题安装参照Next主题介绍"><a href="#主题安装参照Next主题介绍" class="headerlink" title="主题安装参照Next主题介绍"></a>主题安装参照<a href="https://github.com/iissnan/hexo-theme-next" target="_blank" rel="noopener">Next主题介绍</a></h3><h3 id="安装-next-主题"><a href="#安装-next-主题" class="headerlink" title="安装 next 主题"></a>安装 next 主题</h3><pre><code>git clone https://github.com/iissnan/hexo-theme-next.git themes/next</code></pre><h3 id="更改配置文件"><a href="#更改配置文件" class="headerlink" title="更改配置文件"></a>更改配置文件</h3><pre><code># Extensions## Plugins: https://hexo.io/plugins/## Themes: https://hexo.io/themes/theme: next</code></pre><h3 id="站点配置"><a href="#站点配置" class="headerlink" title="站点配置"></a>站点配置</h3><pre><code># Hexo Configuration  站点配置文件## Docs: http://hexo.io/docs/configuration.html## Source: https://github.com/hexojs/hexo/# Sitetitle: HFK                             # 网站标题subtitle: 欢迎到来~                     # 网站副标题description:                           # 网站描述author: HFK                            # 你的昵称language: zh-Hans                      # 网站使用的语言(英文:en, 简体中文:zh-Hans，繁体中文:zh-tw)email: huang0010445341@163.comkeywords: &quot;iOS&quot;timezone: Asia/Shanghai                # 网站时区，用于生成页面时填充相关时间，默认使用电脑时区，一般不用改# URL## If your site is put in a subdirectory, set url as &apos;http://yoursite.com/child&apos; and root as &apos;/child/&apos;## 如果你的站点要放入子目录, 请将url设置为&apos;http://yoursite.com/child&apos; 并将根目录设置为&apos;/child/&apos;url: http://www.hfkstackoverflow.top/        # 站点网址root: /                                      # 网站根目录permalink: :year/:month/:day/:title/         #文单的永久链接格式permalink_defaults:                          # 永久链接中各部分的默认值# Directory# 目录source_dir: source                           # 资源文件夹，这个文件夹用来存放博客内容public_dir: public                           # 公共文件夹，这个文件夹用来存放生成的站点静态文件tag_dir: tags                                # 标签文件夹archive_dir: archives                        # 归档文件夹category_dir: categories                     # 分类文件夹code_dir: downloads/code                     # Include code文件夹i18n_dir: :lang                              # 国际化文件夹，存放各种语言定义skip_render:                                 # 跳过指定文件的渲染# Writingnew_post_name: :title.md # File name of new posts                 # 新文章的文件名称default_layout: post                                              # 预设布局titlecase: false # Transform title into titlecase                 # 把标题转换为 title caseexternal_link: true # Open external links in new tab              # 在新标签中打开链接filename_case: 0                                                  # 把文件名称转换为 (1) 小写或 (2) 大写render_drafts: false                                              # 显示草稿post_asset_folder: false                                          # 启动 Asset 文件夹relative_link: false                                              # 把链接改为与根目录的相对位址future: true                                                      # 显示未来的文章highlight:                                                         # 代码块的设置                                                                                                                                                             enable: true                                                                                                                                                                                                 auto_detect: true                                                                                                                                                                                            line_number: true                                                                                                                                                                                            tab_replace: &apos;&apos;# Category &amp; Tag# 分类 &amp; 标签default_category: uncategorized                        # 默认分类category_map:                                          # 分类别名tag_map:                                               # 标签别名# Archives## 2: Enable pagination## 1: Disable pagination## 0: Fully Disablearchive: 1category: 1tag: 1# Server## Hexo uses Connect as a server## You can customize the logger format as defined in## http://www.senchalabs.org/connect/logger.htmlport: 4000server_ip: localhostlogger: falselogger_format: dev# Date / Time format# 日期 / 时间格式## Hexo uses Moment.js to parse and display date## You can customize the date format as defined in## http://momentjs.com/docs/#/displaying/format/date_format: YYYY-MM-DD       # 日期格式time_format: HH:mm:ss         # 时间格式# Pagination# 分页## 每页显示的文章量 (0 = 关闭分页功能)## Set per_page to 0 to disable paginationper_page: 8pagination_dir: page# Disqusdisqus_shortname:# Extensions## Plugins: https://github.com/hexojs/hexo/wiki/Plugins## Themes: https://github.com/hexojs/hexo/wiki/Themes# plugins:#  - hexo-generator-feed#  - hexo-generator-baidu-sitemap#  - hexo-generator-sitemaptheme: next                 # 主题exclude_generator:# Deployment## Docs: http://hexo.io/docs/deployment.html#deploy:#  type: git#  repository: git@github.com:litten/litten.github.com.git#  branch: masterdeploy:     type: git     repo: https://github.com/huangfangkai/huangfangkai.github.io.git     branch: master</code></pre><h2 id="Next配置"><a href="#Next配置" class="headerlink" title="Next配置"></a>Next配置</h2><h3 id="选择Scheme"><a href="#选择Scheme" class="headerlink" title="选择Scheme"></a>选择Scheme</h3><p>进入主题配置文件，找到 <code>Scheme</code> 字段，Next有4种主题，选择自己喜欢的。</p><pre><code># Schemes  #主题scheme: Muse#scheme: Mist#scheme: Pisces#scheme: Gemini</code></pre><h3 id="设置菜单"><a href="#设置菜单" class="headerlink" title="设置菜单"></a>设置菜单</h3><p>1.进入主题配置文件，找到 <code>menu</code> 字段。</p><pre><code>menu: #菜单 #菜单路径设置 如果hexo在二级目录放置要去掉/  home: / || home  tags: /tags/ || tags  categories: /categories/ || th  archives: /archives/ || archive  about: /about/ || user  #schedule: /schedule/ || calendar  #sitemap: /sitemap.xml || sitemap  #commonweal: /404/ || heartbeat</code></pre><p>2.设定菜单项的图标，对应的字段是 <code>menu_icons</code>。<code>icon name</code> 是 <code>Font Awesome</code> 图标的名字。而 <code>enable</code> 可用于控制是否显示图标，你可以设置成 <code>false</code> 来去掉图标。</p><pre><code># Enable/Disable menu icons.menu_icons:  enable: true  # Icon Mapping.  home: home  about: user  categories: th  tags: tags  archives: archive  commonweal: heartbeat</code></pre><p><strong>注意：</strong> 主题上面一些关于菜单标题可以到 <code>next/languages/</code> 对应文件修改，如下图(改变中文)：</p><p><img src="/assets/blogImg/hexo_blog_Next1.png" alt="菜单文字"></p><h3 id="侧栏设置"><a href="#侧栏设置" class="headerlink" title="侧栏设置"></a>侧栏设置</h3><p>进入主题配置文件，找到 <code>sidebar</code> 字段。</p><pre><code>sidebar: # Sidebar Position, available value: left | right (only for Pisces | Gemini). #position: left  #靠左放置 position: right  #靠右放置 # Sidebar Display, available value (only for Muse | Mist): #  - post    expand on posts automatically. Default. #  - always  expand for all pages automatically #  - hide    expand only when click on the sidebar toggle icon. #  - remove  Totally remove sidebar including sidebar toggle. display: post             # 默认行为，在文章页面（拥有目录列表）时显示 #display: always          #在所有页面中都显示 #display: hide            #在所有页面中都隐藏（可以手动展开） #display: remove          #完全移除</code></pre><h3 id="头像设置"><a href="#头像设置" class="headerlink" title="头像设置"></a>头像设置</h3><p>进入主题配置文件，找到 <code>avatar</code> 字段。</p><pre><code># Sidebar Avatar# in theme directory(source/images): /images/avatar.gif# in site  directory(source/uploads): /uploads/avatar.gifavatar: /images/head.jpeg</code></pre><h3 id="添加「标签」页面"><a href="#添加「标签」页面" class="headerlink" title="添加「标签」页面"></a>添加「标签」页面</h3><pre><code>hexo new page tags</code></pre><p>修改站点目录下 <code>source/tags/index.md</code> 文件如下：</p><p><img src="/assets/blogImg/hexo_blog_Next2.png" alt="hexo_blog_Next2"></p><h3 id="添加「分类」页面"><a href="#添加「分类」页面" class="headerlink" title="添加「分类」页面"></a>添加「分类」页面</h3><pre><code>hexo new page categories</code></pre><p>修改站点目录下 <code>source/categories/index.md</code> 文件如下：</p><p><img src="/assets/blogImg/hexo_blog_Next3.png" alt="hexo_blog_Next3"></p><h3 id="腾讯公益404页面"><a href="#腾讯公益404页面" class="headerlink" title="腾讯公益404页面"></a>腾讯公益404页面</h3><p>腾讯公益404页面，寻找丢失儿童，让大家一起关注此项公益事业！效果如 <a href="http://www.ixirong.com/404.html" target="_blank" rel="noopener">http://www.ixirong.com/404.html</a></p><p>使用方法，新建 404.html 页面，放到主题的 source 目录下，内容如下：</p><pre><code>&lt;!DOCTYPE HTML&gt;&lt;html&gt;&lt;head&gt;  &lt;meta http-equiv=&quot;content-type&quot; content=&quot;text/html;charset=utf-8;&quot;/&gt;  &lt;meta http-equiv=&quot;X-UA-Compatible&quot; content=&quot;IE=edge,chrome=1&quot; /&gt;  &lt;meta name=&quot;robots&quot; content=&quot;all&quot; /&gt;  &lt;meta name=&quot;robots&quot; content=&quot;index,follow&quot;/&gt;&lt;/head&gt;&lt;body&gt;&lt;script type=&quot;text/javascript&quot; src=&quot;http://www.qq.com/404/search_children.js&quot;        charset=&quot;utf-8&quot; homePageUrl=&quot;your site url &quot;        homePageName=&quot;回到我的主页&quot;&gt;&lt;/script&gt;&lt;/body&gt;&lt;/html&gt;</code></pre><h3 id="侧边栏社交链接"><a href="#侧边栏社交链接" class="headerlink" title="侧边栏社交链接"></a>侧边栏社交链接</h3><p>进入主题配置文件，找到 <code>Social Links</code> 字段。</p><pre><code># Social Links.# Usage: `Key: permalink || icon`# Key is the link label showing to end users.# Value before `||` delimeter is the target permalink.# Value after `||` delimeter is the name of FontAwesome icon. If icon (with or without delimeter) is not specified, globe icon will be loaded.social: # 添加你的社交账号  #LinkLabel: Link  GitHub: https://github.com/huangfangkai || github  简书: https://www.jianshu.com/u/e5c418741dce  #E-Mail: mailto:yourname@gmail.com || envelope  #Google: https://plus.google.com/yourname || google  #Twitter: https://twitter.com/yourname || twitter  #FB Page: https://www.facebook.com/yourname || facebook  #VK Group: https://vk.com/yourname || vk  #StackOverflow: https://stackoverflow.com/yourname || stack-overflow  #YouTube: https://youtube.com/yourname || youtube  #Instagram: https://instagram.com/yourname || instagram  #Skype: skype:yourname?call|chat || skype</code></pre><h2 id="Next主题美化"><a href="#Next主题美化" class="headerlink" title="Next主题美化"></a>Next主题美化</h2><h3 id="文章中添加居中模块"><a href="#文章中添加居中模块" class="headerlink" title="文章中添加居中模块"></a>文章中添加居中模块</h3><p>在写文章时添加如下：</p><pre><code>&lt;blockquote class=&quot;blockquote-center&quot;&gt;以梦为马&lt;/blockquote&gt;</code></pre><p><img src="/assets/blogImg/hexo_blog_Next4.png" alt="hexo_blog_Next4"></p><h3 id="在文章底部增加版权信息"><a href="#在文章底部增加版权信息" class="headerlink" title="在文章底部增加版权信息"></a>在文章底部增加版权信息</h3><p>1.在目录 <code>next/layout/_macro/</code> 下添加 <code>my-copyright.swig</code>，写入如下：</p><pre><code>{% if page.copyright %}<div class="my_post_copyright">  <script src="//cdn.bootcss.com/clipboard.js/1.5.10/clipboard.min.js"></script>  <!-- JS库 sweetalert 可修改路径 -->  <script type="text/javascript" src="http://jslibs.wuxubj.cn/sweetalert_mini/jquery-1.7.1.min.js"></script>  <script src="http://jslibs.wuxubj.cn/sweetalert_mini/sweetalert.min.js"></script>  <link rel="stylesheet" type="text/css" href="http://jslibs.wuxubj.cn/sweetalert_mini/sweetalert.mini.css">  <p><span>本文标题:</span><a href="{{ url_for(page.path) }}">{{ page.title }}</a></p>  <p><span>文章作者:</span><a href="/" title="访问 {{ theme.author }} 的个人博客">{{ theme.author }}</a></p>  <p><span>发布时间:</span>{{ page.date.format("YYYY年MM月DD日 - HH:MM") }}</p>  <p><span>最后更新:</span>{{ page.updated.format("YYYY年MM月DD日 - HH:MM") }}</p>  <p><span>原始链接:</span><a href="{{ url_for(page.path) }}" title="{{ page.title }}">{{ page.permalink }}</a>    <span class="copy-path" title="点击复制文章链接"><i class="fa fa-clipboard" data-clipboard-text="{{ page.permalink }}" aria-label="复制成功！"></i></span>  </p>  <p><span>许可协议:</span><i class="fa fa-creative-commons"></i> <a rel="license" href="https://creativecommons.org/licenses/by-nc-nd/4.0/" target="_blank" title="Attribution-NonCommercial-NoDerivatives 4.0 International (CC BY-NC-ND 4.0)">署名-非商业性使用-禁止演绎 4.0 国际</a> 转载请保留原文链接及作者。</p>  </div><script>     var clipboard = new Clipboard('.fa-clipboard');    clipboard.on('success', $(function(){      $(".fa-clipboard").click(function(){        swal({             title: "",             text: '复制成功',             html: false,          timer: 500,             showConfirmButton: false        });      });    }));  </script>{% endif %}</code></pre><p>2.在目录<code>next/source/css/_common/components/post/</code>下添加<code>my-post-copyright.styl</code>，写入如下：</p><pre><code>.my_post_copyright {  width: 85%;  max-width: 45em;  margin: 2.8em auto 0;  padding: 0.5em 1.0em;  border: 1px solid #d3d3d3;  font-size: 0.93rem;  line-height: 1.6em;  word-break: break-all;  background: rgba(255,255,255,0.4);}.my_post_copyright p{margin:0;}.my_post_copyright span {  display: inline-block;  width: 5.2em;  color: #b5b5b5;  font-weight: bold;}.my_post_copyright .raw {  margin-left: 1em;  width: 5em;}.my_post_copyright a {  color: #808080;  border-bottom:0;}.my_post_copyright a:hover {  color: #a3d2a3;  text-decoration: underline;}.my_post_copyright:hover .fa-clipboard {  color: #000;}.my_post_copyright .post-url:hover {  font-weight: normal;}.my_post_copyright .copy-path {  margin-left: 1em;  width: 1em;  +mobile(){display:none;}}.my_post_copyright .copy-path:hover {  color: #808080;  cursor: pointer;}</code></pre><p>  3.修改<code>next/layout/_macro/post.swig</code>，在代码</p><pre><code>{% if theme.wechat_subscriber.enabled and not is_index %}      <div>        {% include 'wechat-subscriber.swig' %}      </div>  {% endif %}</code></pre><p>之前添加增加如下代码： </p><pre><code>&lt;div&gt;     {% if not is_index %} {% include 'my-copyright.swig' %} {% endif %} &lt;/div&gt;</code></pre><p> 4.修改<code>next/source/css/_common/components/post/post.styl</code>文件，在最后一行增加代码：</p><pre><code>@import &quot;my-post-copyright&quot;</code></pre><p>5.如果要在该博文下面增加版权信息的显示，需要在 Markdown 中增加<code>copyright: true</code>的设置，类似：</p><pre><code>---title: date: tags: categories: copyright: true---</code></pre><p><strong>注意：</strong>为了方便不用每次添加<code>copyright: true</code>，可以在<code>scaffolds/post.md</code>中设置</p><pre><code>title: {{ title }}date: {{ date }}tags:categories: notshow: falsecopyright: true---</code></pre><h3 id="隐藏网页底部powered-By-Hexo-强力驱动"><a href="#隐藏网页底部powered-By-Hexo-强力驱动" class="headerlink" title="隐藏网页底部powered By Hexo / 强力驱动"></a>隐藏网页底部powered By Hexo / 强力驱动</h3><p>打开<code>themes/next/layout/_partials/footer.swig</code>,使用””隐藏之间的代码即可，或者直接删除。</p><pre><code>&lt;!--&lt;div class=&quot;powered-by&quot;&gt;  {{ __('footer.powered', '<a class="theme-link" rel="noopener" href="https://hexo.io" target="_blank">Hexo</a>') }}&lt;/div&gt;&lt;div class=&quot;theme-info&quot;&gt;  {{ __('footer.theme') }} -  &lt;a class=&quot;theme-link&quot; rel=&quot;external nofollow&quot; href=&quot;https://github.com/iissnan/hexo-theme-next&quot;&gt;    NexT.{{ theme.scheme }}  &lt;/a&gt;&lt;/div&gt;--&gt;</code></pre><h3 id="文章加密访问"><a href="#文章加密访问" class="headerlink" title="文章加密访问"></a>文章加密访问</h3><p>打开<code>themes-&gt;next-&gt;layout-&gt;_partials-&gt;head.swig</code>文件,在<code>meta</code>标签后面插入这样一段代码：</p><pre><code>&lt;script&gt;    (function(){        if(&apos;{{ page.password }}&apos;){            if (prompt(&apos;请输入文章密码&apos;) !== &apos;{{ page.password }}&apos;){                alert(&apos;密码错误！&apos;);                history.back();            }        }    })();&lt;/script&gt;</code></pre><p>然后文章中添加：</p><pre><code>password: nmask</code></pre><h3 id="博文置顶"><a href="#博文置顶" class="headerlink" title="博文置顶"></a>博文置顶</h3><p>修改 <code>hero-generator-index</code> 插件，把文件：<code>node_modules/hexo-generator-index/lib/generator.js</code> 内的代码替换为：</p><pre><code>&apos;use strict&apos;;var pagination = require(&apos;hexo-pagination&apos;);module.exports = function(locals){  var config = this.config;  var posts = locals.posts;    posts.data = posts.data.sort(function(a, b) {        if(a.top &amp;&amp; b.top) { // 两篇文章top都有定义            if(a.top == b.top) return b.date - a.date; // 若top值一样则按照文章日期降序排            else return b.top - a.top; // 否则按照top值降序排        }        else if(a.top &amp;&amp; !b.top) { // 以下是只有一篇文章top有定义，那么将有top的排在前面（这里用异或操作居然不行233）            return -1;        }        else if(!a.top &amp;&amp; b.top) {            return 1;        }        else return b.date - a.date; // 都没定义按照文章日期降序排    });  var paginationDir = config.pagination_dir || &apos;page&apos;;  return pagination(&apos;&apos;, posts, {    perPage: config.index_generator.per_page,    layout: [&apos;index&apos;, &apos;archive&apos;],    format: paginationDir + &apos;/%d/&apos;,    data: {      __index: true    }  });};</code></pre><p>在文章中添加 top 值，数值越大文章越靠前，如:</p><pre><code>---......copyright: truetop: 100---</code></pre><h3 id="修改文章底部的那个带-号的标签"><a href="#修改文章底部的那个带-号的标签" class="headerlink" title="修改文章底部的那个带#号的标签"></a>修改文章底部的那个带#号的标签</h3><p>修改模板<code>/themes/next/layout/_macro/post.swig</code>，搜索 <code>rel=&quot;tag&quot;&gt;#</code>，将 <code>#</code> 换成<code>&lt;i class=&quot;fa fa-tag&quot;&gt;</code></p><h2 id="Next主题美化-1"><a href="#Next主题美化-1" class="headerlink" title="Next主题美化"></a><strong>Next主题美化</strong></h2><p><a href="https://www.jianshu.com/p/1f8107a8778c" target="_blank" rel="noopener">hexo搭建个人博客–NexT主题优化</a></p><p><a href="https://thief.one/2017/03/03/Hexo%E6%90%AD%E5%BB%BA%E5%8D%9A%E5%AE%A2%E6%95%99%E7%A8%8B/" target="_blank" rel="noopener">Hexo搭建博客教程</a></p><p><a href="https://www.jianshu.com/p/d95cff938277" target="_blank" rel="noopener">Hexo博客搭建全攻略(二)：NexT主题配置</a></p><h2 id="SEO优化"><a href="#SEO优化" class="headerlink" title="SEO优化"></a><strong>SEO优化</strong></h2><p><code>seo</code>优化对于网站是否能被搜索引擎快速收录有很大帮助，因此适当做一些seo还是有必要的，以下内容参考：</p><p>1.添加sitemap文件<br>安装以下2个插件，然后重启hexo后，网站根目录（source）下会生成sitemap.xml与baidusitemap.xml文件，搜索引擎在爬取时会参照文件中的url去收录。</p><pre><code>npm install hexo-generator-sitemap --save-devhexo d -gnpm install hexo-generator-baidu-sitemap --save-devhexo d -g</code></pre><p>2.添加robots.txt<br>新建robots.txt文件，添加以下文件内容，把robots.txt放在hexo站点的source文件下。</p><pre><code>User-agent: * Allow: /Allow: /archives/Disallow: /vendors/Disallow: /js/Disallow: /css/Disallow: /fonts/Disallow: /vendors/Disallow: /fancybox/Sitemap: http://thief.one/sitemap.xmlSitemap: http://thief.one/baidusitemap.xml</code></pre><p><a href="https://blog.paddings.cn/2016/08/16/blog/Hexo-NexT-SEO/" target="_blank" rel="noopener">Hexo NexT 主题SEO优化指南</a></p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;&lt;strong&gt;前言&lt;/strong&gt;&lt;/h2&gt;&lt;p&gt;通过一个方式来整理和管理自己的知识，之前了解到可以通过 GitHub 托管静态网页，使用 Hexo 和 GitHub Pages 方便快捷地搭建个人博客网站，所以通过学习从零开始搭建、配置一个属于自己的博客网站。&lt;/p&gt;
    
    </summary>
    
      <category term="博客搭建" scheme="http://www.hfkstackoverflow.top/categories/%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/"/>
    
    
      <category term="hexo" scheme="http://www.hfkstackoverflow.top/tags/hexo/"/>
    
  </entry>
  
</feed>
